# In-App Reader + AI Chat

Built-in reader mode (readability.js) for clean article viewing. AI chat per article with full context — user selects text, asks questions, gets Egyptian Arabic explanations. Chosen over external browser to keep the reading/annotation/chat loop inside the app. Highlights and notes persist across sessions.
