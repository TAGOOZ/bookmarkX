# SQLite + Supabase for Data Storage

Local-first SQLite for speed and zero-config. Supabase (Postgres) for cloud sync to enable future mobile access. sqlite-vec for local vector embeddings, pgvector for cloud. Chosen over Turso (user unfamiliar) and pure cloud DB (needs offline support).
