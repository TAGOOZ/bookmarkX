# CSS Modules for Styling

CSS Modules for React component styling — scoped CSS with no runtime overhead. Chosen over Tailwind (user wants pixel-perfect Obsidian-like design control) and styled-components (runtime cost). Enables custom design tokens for dark/light mode themes.
